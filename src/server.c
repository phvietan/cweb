#include <sys/socket.h>
#include <stdio.h>
#include "../include/constants.h"

int main(int argc, char * argv[], char * envp[]) {
    puts("wtf");
    puts("lalaala");
    printf("%d\n", PORT);
    return 0;
}